function totalScore = totalScoreUpDown(m_matrix)
%Compute total score, include columns with connection confliction
    [M,N] = size(m_matrix);
    totalScore = 0;
    for column = 1:N
        scoreOfThisColumn = 0;
        
        totalScore = totalScore + scoreOfThisColumn;
    end

end